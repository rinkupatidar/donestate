import { OPACITY_DELAY_VARIANT } from "./utils/variants";
import { OPACITY_VARIANTS } from "./variants";

export const addOpacityAnimation = {
	initial: "initial",
	animate: "animate",
	exit: "exit",
	variants: OPACITY_VARIANTS,
};

export const addOpacityDelayAnimation = {
	initial: "initial",
	animate: "animate",
	exit: "exit",
	variants: OPACITY_DELAY_VARIANT,
};
